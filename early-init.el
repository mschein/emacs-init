;;
;; Link to this file with:
;; mkdir -p ~/.emacs.d/
;; ln -s ~/emacs-init/early-init.el ~/.emacs.d/early-init.el
;;
;; Disable the normal package system so we can use elpaca.
(setq package-enable-at-startup nil)
